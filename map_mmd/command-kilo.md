---
description: Build or query a map.mmd knowledge graph
---

Invoke the `map.mmd` skill immediately.

Pass the full `/map.mmd` argument string through unchanged.
If no arguments were supplied, treat the target path as `.`.

Examples:
- `/map.mmd`
- `/map.mmd src --update`
- `/map.mmd query "what connects auth to billing?"`

Do not answer from raw files before handing off to the `map.mmd` skill.
